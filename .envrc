use flake  ~/nixos-config#py_with_cuda

if [ ! -d .venv ]; then
  uv venv .venv
fi
source .venv/bin/activate

# CUDA: driver stub (libcuda.so.1) + runtime (libcudart.so.12)
# CUDA_TOOLKIT=$(ls -d /nix/store/*-cuda-merged-*/lib 2>/dev/null | head -1)
# export LD_LIBRARY_PATH=/run/opengl-driver/lib${CUDA_TOOLKIT:+:$CUDA_TOOLKIT}:$LD_LIBRARY_PATH